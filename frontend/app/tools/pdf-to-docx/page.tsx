'use client';
import Link from 'next/link'; import { ArrowLeft, FileText } from 'lucide-react'; import { FileUploader } from '../../../components/FileUploader';
export default function PdfToDocx() { return <main className="min-h-screen bg-paper px-6 py-8"><div className="mx-auto max-w-3xl"><Link href="/" className="text-sm text-slate-500"><ArrowLeft className="mr-2 inline" size={15} /> Back to toolkit</Link><div className="mt-20"><FileText className="text-coral" size={32} /><h1 className="mt-5 font-display text-6xl">PDF to DOCX</h1><p className="mt-4 max-w-xl text-lg text-slate-500">Turn locked layouts into editable documents while keeping the structure close to the original.</p><div className="mt-10"><FileUploader accept="application/pdf" multiple={false} onFiles={(files) => console.log(files)} /></div></div></div></main>; }
